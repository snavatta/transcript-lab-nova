services:
  transcriptlab:
    build:
      dockerfile: Dockerfile.openvino-genai
    devices:
      - /dev/dri:/dev/dri
    environment:
      - Transcription__OpenVinoGenAi__Device=${OPENVINO_DEVICE:-GPU}
      - Transcription__OpenVinoWhisperSidecar__Device=${OPENVINO_DEVICE:-GPU}
