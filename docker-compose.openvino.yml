services:
  transcriptlab:
    build:
      dockerfile: Dockerfile.openvino
    devices:
      - /dev/dri:/dev/dri
    environment:
      - Transcription__OpenVinoWhisperSidecar__Device=${OPENVINO_DEVICE:-GPU}
